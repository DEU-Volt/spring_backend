spring:
  application:
    name: eureka-server
  boot:
    admin:
      client:
        url: http://127.0.0.1:8081
  config:
    activate:
      on-profile: eureka-master

management:
  endpoints:
    web:
      exposure:
        include: "*"

server:
  port: 8761

eureka:
  instance:
    hostname: localhost
  client:
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: http://localhost2:8762/eureka/
      # defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/

  # from: https://github.com/spring-cloud-samples/eureka/blob/master/src/main/resources/application.yml
  server:
#    waitTimeInMsWhenSyncEmpty: 0
    response-cache-update-interval-ms: 5000

---

spring:
  application:
    name: eureka-server
  boot:
    admin:
      client:
        url: http://localhost:8081
  config:
    activate:
      on-profile: eureka-replication

management:
  endpoints:
    web:
      exposure:
        include: "*"

server:
  port: 8762

eureka:
  instance:
    hostname: localhost2
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

  # from: https://github.com/spring-cloud-samples/eureka/blob/master/src/main/resources/application.yml
  server:
    #    waitTimeInMsWhenSyncEmpty: 0
    response-cache-update-interval-ms: 5000


