spring:
  profiles:
    active: dev
  application:
    name: product-service
  cloud:
    config:
      uri: http://localhost:8088

management:
  endpoints:
    web:
      exposure:
        include: "*"