spring:
  application:
    name: gateway
  boot:
    admin:
      client:
        url: http://localhost:8081

spring.security.oauth2.resourceserver.jwt.issuer-uri: http://localhost:9000


management:
  endpoints:
    web:
      exposure:
        include: "*"


